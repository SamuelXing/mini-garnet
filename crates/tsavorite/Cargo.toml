[package]
name = "tsavorite"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Mini Tsavorite: epoch-protected hash index over a hybrid (memory + disk) record log with a narrow RUMDS interface"

[dependencies]
